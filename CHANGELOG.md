# 更新记录

版本级的完整记录仍以应用内更新日志、[当前程序情况](docs/当前程序情况.md) 和 GitHub Releases 为准。

## [7.6] - 2026-07-27

- 修复 AI 回复的两类截断：主助手“全屏阅读”不再使用预览长度；分类、湿重、主助手的正文可完整滚动查看。
- 分类/湿重首轮输出从 700 恢复为 2400 token，统一 API 任务会在服务端返回 length 或只返回思考过程时自动续写，优先补齐最终结论与结构化标记。
- 兼容服务商重复返回 reasoning_content/正文的场景，避免思考过程和可见答案重复展示。
- 修复 Apilot 已安装却被 resolveActivity 预检误判为未安装的问题：导入/发送直接启动真实 V2 Intent，仅在实际启动失败时才引导安装。

## [7.5] - 2026-07-27

- AI 结果统一拆分为“用户可见答案”和可折叠“思考过程”：兼容 think 标签、OpenAI 兼容的 reasoning_content 及中文思考段；默认不让推理正文挤占分类、湿重或助手结论。
- 分类/湿重的最终分类、最终湿重和候选数值仅从可见答案解析；提示词默认折叠，避免长提示词和思考内容把结论截断。
- Apilot V2 发送改为逐条可选的真实 payload；未检测到 Apilot 时引导用户访问其 GitHub 项目。

## [7.3] - 2026-07-27

- 主 App 全局助手迁移到语音助手 v2 Binder 会话：先发现能力，再按真实调用方签名与用户授权范围建立会话。
- 仅在受 `domain_profile` 授权时提供当前数据集物种词表；规范化文本仍进入既有动作预览，绝不绕过确认直接写入。
- 主 App 与语音助手 Release 使用同一正式证书，并同步发布说明、加密离线文档与签名核验信息。

## [voice-v3.7] - 2026-07-27

- 语音助手重构为受权限与签名校验保护的 v2 Binder 会话；通用合作方与 `plankton-v1` 领域 Profile 有明确边界，转写结果只形成建议，仍由主 App 预览确认。
- 后台转写任务持久化，支持精确取消、进程中断恢复、24 小时音频引用清理、脱敏诊断和无真实数据的评分基准。
- 升级语音助手目标 API 至 36（Android 16），最低 API 仍为 34（Android 14）；正式包使用主 App 同一证书签名。

## [7.2] - 2026-07-27

- 检查 GitHub 更新的结果改为在操作按钮下即时显示；当前版本已为最新时会明确提示，不再把状态放在设置页底部。
- 设置页移除遗留的总“保存”按钮。默认原水体积在输入完成或离开输入框时热保存；无效值不会覆盖已保存设置。
- 应用内更新日志只给 `7.2` 标记“当前”，清除历史版本错误的“当前”标记。

## [7.1] - 2026-07-27

- 专注录入改为整页连续滚动；物种名称支持两行显示以及点按/长按查看完整中英文名。
- 图片识别的 API1、API2 与图片服务改为纵向选择，配置错误不再因横向排版被截断。
- Web 依赖与锁文件升级；Android/语音依赖自动更新只保留兼容补丁，跨 compileSdk/AGP 的升级单独处理。

## [7.0] - 2026-07-27

- 主 App 发布工作会话栏、普通页快速计数、手动步进、多图冲突确认、图片任务状态/取消和采样解码优化。
- 发布签名、更新检查、文档同步和恢复快照流程固化。

后续版本请同时更新本文件、应用内更新日志、项目书和对应 Release 说明。
