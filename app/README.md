# 前端应用（Vite + React + TS）

## 开发
- `npm install`
- `npm run generate:wetweights`
- `npm run dev`

## 构建
- `npm run build`

## 脚本
- `npm run generate:wetweights`：从仓库根目录的 `表三.xlsx` 生成 `src/data/wetweights.json`
- `npm run dev:export`：生成两份演示导出文件到 `d:/plankton/out/`

