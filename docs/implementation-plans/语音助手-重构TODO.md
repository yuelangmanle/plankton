# 语音助手重构 TODO

关联方案：[语音助手产品与工程重构实施计划](语音助手-产品与工程重构实施计划.md)。

## 发布规则

- [ ] `3.5`、`3.6`、`3.7` 只作为内部代码里程碑：运行定向测试、lint 和编译，不递增版本、不打中间 APK、不更新加密文档、不创建 Release。
- [ ] 全部任务通过后，仅发布一次语音助手 `v3.7 (37)`：正式签名、文档同步、加密资源、APK 签名/哈希核验和 GitHub Release 一次完成。

## 3.5：开放接入与可靠性底座

- [ ] 建立单一 v2 契约、JSON 向量和两端兼容测试；明确能力发现、错误码、尺寸限制、取消幂等性、保留期和 v1 迁移。
- [ ] 实现通用授权伙伴 API：真实 UID/包名/签名识别、一次性或限时范围授权、会话与回调，关闭导出的裸 `TranscribeService`。
- [ ] 浮游动物一体化使用同一 v2 API 的 `plankton-v1` Profile；普通第三方使用 `generic`，不获得领域上下文或音频回传权限。
- [ ] 任务、取消、恢复和历史状态持久化；进程重建后可继续或取消。
- [ ] 音频缓存 24 小时自动清理；历史、缓存、脱敏诊断独立清除；禁止自动备份语音和授权数据。

## 3.6：现场体验与代码收敛

- [ ] 抽取共享 `TranscriptionController` 和 UI 状态，删除主页面/接入页面重复的录音、队列、结果与资源释放代码。
- [ ] 提供快速发送与核对后发送两种模式；始终由接收 App 确认写库。
- [ ] 重做悬浮录音：按住、锁定、丢弃、停止、音量/时长、触感/声音反馈、可访问性标签。
- [ ] 结果时间线、低置信词、录音质量、重试/取消、编辑文本和命令审阅统一呈现。
- [ ] 将浮游动物别名、点位和计数规范化收敛到 Profile；其他 App 用受限 Profile JSON 扩展，不允许注入代码。

## 3.7：质量门禁与唯一发布

- [ ] 构造无真实用户数据的语料与动作正确率回归；记录 CER、动作完全匹配率、首段文本时间、p95 耗时、回退原因和峰值内存。
- [ ] Android 14-17 权限、前台服务、悬浮窗、锁屏转写、低内存、正式包覆盖和跨 App 调用验收。
- [ ] 集中迁移 `compileSdk/targetSdk`、AGP/AndroidX 兼容组合，修复所有 lint/系统行为问题。
- [x] 语音助手设置页新增“检查 GitHub 更新”：与主 App 保持一致，显示检查中/当前已是最新版本/失败原因；仅筛选 `voice-v*` 标签和 APK 附件，发现正式语音助手 Release 后展示说明并交由系统下载器下载。正式 `voice-v3.7` 发布仍在全部 v3.7 门禁完成后进行。
- [ ] 唯一正式发布：升级 `3.7 (37)`，同主 App 证书签名，更新日志/项目书/加密文档/Release 说明，上传 APK。
